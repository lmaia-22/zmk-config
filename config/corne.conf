# OLED I2C (GND VCC SCL SDA) — ecra de estado do Corne
CONFIG_ZMK_DISPLAY=y

# Poupanca de energia
CONFIG_ZMK_SLEEP=y
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=360000

# Alcance BT
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

CONFIG_ZMK_KEYBOARD_NAME="Corno"
